{
  "name": "recourse-example-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-index.mjs && node build-widget.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@recourse-ai/core": "workspace:*",
    "ai": "^7.0.91"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^5.20260902.1",
    "typescript": "^5.9.3"
  }
}
