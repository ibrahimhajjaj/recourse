{
  "name": "@helpdeck/evals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Scored suites for answer quality, retrieval and injection resistance. Not published.",
  "bin": {
    "helpdeck-eval": "./src/cli.ts"
  },
  "scripts": {
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "eval": "tsx src/cli.ts"
  },
  "dependencies": {
    "helpdeck": "workspace:*"
  },
  "devDependencies": {
    "@ai-sdk/openai-compatible": "^3.0.40",
    "@types/node": "^22.10.5",
    "ai": "^7.0.84",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
